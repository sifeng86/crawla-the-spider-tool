version: "3.5"
services:
  crawla_web:
    build: .
    ports:
      - "3000:3000"
    volumes: 
      - ".:/work"
    networks:
      - crw
  chrome:
    image: "selenium/standalone-chrome:latest"
    ports:
      - "4444:4444"
    networks:
      - crw
  redis:
    image: redis:6.2-alpine
    networks:
      - crw

networks:
  crw:
    driver: bridge
